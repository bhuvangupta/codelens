# CodeLens UI Environment Variables
# Copy this file to .env and fill in your values

# ===================
# Server Configuration
# ===================
PORT=5175
HOST=0.0.0.0
NODE_ENV=development

# ===================
# Backend Configuration
# ===================
# BACKEND_URL: Internal backend URL (server-to-server, used by SvelteKit SSR proxy)
# - Development: http://localhost:9292
# - Production: http://<internal-ip>:9292 or http://backend:9292 (Docker)
BACKEND_URL=http://localhost:9292

# PUBLIC_API_URL: External backend URL (browser direct calls - WebSockets, uploads)
# - Development: http://localhost:9292
# - Production: https://yourdomain.com/api (if nginx routes /api to backend)
#               or https://api.yourdomain.com (separate API domain)
PUBLIC_API_URL=http://localhost:9292

# PUBLIC_CODEMEDIC_PORT: Port for CodeMedic backend
PUBLIC_CODEMEDIC_PORT=8000

# ===================
# Login Providers
# ===================
# Enable/disable OAuth providers (shown on login page)
PUBLIC_GOOGLE_LOGIN_ENABLED=true
PUBLIC_GITHUB_LOGIN_ENABLED=false

# ===================
# Production Notes
# ===================
# For production with nginx routing /api/* and /oauth2/* to backend:
# - OAuth uses relative path (/oauth2/authorization/google)
# - Session cookies work (same domain)
# - Set BACKEND_URL to internal backend address for SSR proxy

# ===================
# Deployment (optional)
# ===================
# Docker Registry for pushing images
DOCKER_REGISTRY=

# Server deployment via SSH
SERVER_HOST=
SERVER_USER=deploy
DEPLOY_DIR=/opt/codelens-ui
